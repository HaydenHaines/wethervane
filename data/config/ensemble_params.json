{
  "_comment": "HGB hyperparameters and ensemble blend weights for train_ensemble_model.py",
  "_tuning_notes": "Blend weights 0.5/0.5 were chosen empirically; tuning via CV is deferred to post-MVP. HGB params updated 2026-03-26: max_iter 300->800, lr 0.05->0.1, max_depth 4->5, min_samples_leaf 20->30",
  "hgb": {
    "max_iter": 800,
    "learning_rate": 0.1,
    "max_depth": 5,
    "min_samples_leaf": 30,
    "l2_regularization": 1.0,
    "random_state": 42
  },
  "ensemble": {
    "ridge_weight": 0.5,
    "hgb_weight": 0.5
  }
}
